package data;

/**
 * Global progression flags persisted via save/load.
 * Used to gate spawns, cutscenes, and one-time events.
 */
public class Progress {
    public static boolean bossMonsterDefeated;
}
